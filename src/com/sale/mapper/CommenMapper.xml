<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sale.mapper.CommenMapper">
	<select id="calTotal" resultType="double">
		select sum(b_total) from tb_bill where b_status = 1
	</select>
</mapper>